# GreenThumb Agent 🪴
Personal Plant Care Assistant built for the Kaggle Capstone.

## Setup
1. Clone this repo.
2. Install requirements: `pip install -r requirements.txt`
3. Set your `GEMINI_API_KEY` environment variable.
4. Run `python app.py` to start the web interface.
